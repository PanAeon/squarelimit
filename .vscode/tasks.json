{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "diag",
            "type": "shell",
            "command": "stack run  -- --selection limit  -w 600 -h 600 -o output.svg",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}